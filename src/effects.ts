// ============================================================
// wavy-bavy/effects — Effects barrel export
// ============================================================

// Effect types
export type {
    ShadowConfig,
    GlowConfig,
    StrokeConfig,
    BlurConfig,
    TextureConfig,
    InnerShadowConfig,
} from './types'

// Default effect configs
export {
    DEFAULT_SHADOW,
    DEFAULT_GLOW,
    DEFAULT_STROKE,
    DEFAULT_BLUR,
    DEFAULT_TEXTURE,
    DEFAULT_INNER_SHADOW,
} from './constants'
